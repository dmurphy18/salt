Jinja2

## msgpack>=0.5,!=0.5.5,<1.0.0
# Debian 10 has msgpack 1.0.0, had to adjust Salt and it was the beta 1.0.0 had issues 
msgpack > 0.5.5

PyYAML
MarkupSafe
requests>=1.0.0
# Requirements for Tornado 4.5.3 (vendored as salt.ext.tornado)
backports-abc==0.5; python_version < '3.0'
singledispatch==3.4.0.3; python_version < '3.4'
# Required by Tornado to handle threads stuff.
futures>=2.0; python_version < '3.0'
